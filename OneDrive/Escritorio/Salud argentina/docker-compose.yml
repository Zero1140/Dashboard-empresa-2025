services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./app:/app/app
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-saludos}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-saludos_dev}
      POSTGRES_DB: ${POSTGRES_DB:-saludos_db}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations/sql/001_initial_schema.sql:/docker-entrypoint-initdb.d/001_initial_schema.sql
      - ./migrations/sql/002_add_consultations.sql:/docker-entrypoint-initdb.d/002_add_consultations.sql
      - ./migrations/sql/003_add_practitioner_invitations.sql:/docker-entrypoint-initdb.d/003_add_practitioner_invitations.sql
      - ./migrations/sql/004_consent_and_refresh_tokens.sql:/docker-entrypoint-initdb.d/004_consent_and_refresh_tokens.sql
      - ./migrations/sql/005_consent_events.sql:/docker-entrypoint-initdb.d/005_consent_events.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-saludos}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  celery:
    build: .
    command: .venv/bin/celery -A app.worker worker --loglevel=info
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
